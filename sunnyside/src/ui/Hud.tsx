/**
 * Hud.tsx — тонкий ре-экспорт (совместимость пути импорта в `App.tsx`).
 * Реализация — зона `hud-nav`, см. `./hud/HudRoot.tsx` и соседние файлы там.
 */

export { HudRoot as Hud } from './hud/HudRoot'
