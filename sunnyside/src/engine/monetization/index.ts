/**
 * engine/monetization — барель системы монетизации (docs/specs/15-monetization.md).
 * Ноль three/react/net — только `@/types` и `@/engine/contracts` (AGENTS.md §3).
 */

export { createMonetizationSystem } from './system'
