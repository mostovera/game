/**
 * demo.ts — константа UI-only ожидания роста для оптимистичного отклика сцены фермы.
 *
 * ЗАЧЕМ: при клике «посеять/полить» до подтверждения сервера сцена (`systems.tsx`)
 * оптимистично показывает грядку растущей на `DEMO_GROW_MS`, чтобы отклик был мгновенным.
 * Это НЕ эконом-формула и НЕ источник начисления — серверный `readyAt` перезапишет её при
 * гидрации после подтверждения мутации (AGENTS.md §0.3, истина — сервер).
 *
 * Демо-снапшот фермы (`demoFarmSnapshot`) УДАЛЁН: слайс `farm` наполняется реальной
 * гидрацией из адаптера на бутстрапе (`app/backend.ts` `bootstrap` → `getFarm` → `setFarm`),
 * а не синтетическим сидом — сцена рисует истинное состояние локального/серверного мира.
 *
 * ГРАНИЦА: чистые данные, ноль three/react.
 */

/** UI-only длительность роста при оптимистичном севе (не эконом-формула; сервер даёт свой readyAt). */
export const DEMO_GROW_MS = 8 * 60 * 1000
