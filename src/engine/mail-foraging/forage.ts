/**
 * engine/mail-foraging/forage.ts — чистые формулы мира фуражинга (08-mail-foraging
 * §3.2.2/§3.2.3): индекс «фуражинг-суток» (респавн/сброс лимитов в 06:00 UTC, НЕ
 * совпадает с недельным ролловером 00:00 UTC понедельника, см. открытый вопрос §8.6
 * спеки — оставлен как задача калибровки, не конфликт).
 *
 * ГРАНИЦА (AGENTS.md §3): ноль three/react/net/state — чистая математика,
 * node-тестируема без мокания времени адаптера.
 */

import type { EpochMs } from '@/types'
import { DAY_MS, FORAGE_RESPAWN_OFFSET_MS } from './constants'

/**
 * Индекс суток фуражинга для момента `t`: сутки начинаются в 06:00 UTC (не в
 * полночь) — целые сутки от `FORAGE_RESPAWN_OFFSET_MS` эпохи. Два разных момента
 * времени дают одинаковый индекс тогда и только тогда, когда между ними не было
 * пересечения границы 06:00 UTC — используется адаптером для идемпотентного
 * детекта «наступил новый день фуражинга» (респавн пула + сброс личных лимитов).
 */
export function forageDayIndex(t: EpochMs): number {
  return Math.floor((t - FORAGE_RESPAWN_OFFSET_MS) / DAY_MS)
}
