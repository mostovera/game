/**
 * engine/animals — барель системы животных (docs/specs/03-animals.md).
 * Чистая логика: циклы кормления, продукты, привязанность. Ноль three/react/net —
 * только `@/types` и `@/engine/contracts` (AGENTS.md — граница `engine/ → types/`).
 */

export { createAnimalSystem } from './system'
export * from './formulas'
